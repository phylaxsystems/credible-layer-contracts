---
phase: 04-documentation-abi-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - artifacts/StateOracle.json
  - artifacts/DAVerifierECDSA.json
  - artifacts/AdminVerifierOwner.json
  - artifacts/interfaces/IDAVerifier.json
  - artifacts/interfaces/IAdminVerifier.json
  - artifacts/interfaces/IBatch.json
  - artifacts/libraries/AdminVerifierRegistry.json
  - README.md
autonomous: true
requirements:
  - R19
  - R20

must_haves:
  truths:
    - "npm run prepare succeeds and artifacts directory contains updated StateOracle ABI with addDAVerifier, removeDAVerifier, isDAVerifierRegistered, updated addAssertion(5 params), DAVerifierAdded, DAVerifierRemoved, DAVerifierNotRegistered, InvalidDAProof"
    - "README documents DA verifier registry concept: governance can add/remove DA verifiers"
    - "README documents per-assertion DA verifier selection in addAssertion"
    - "README documents DAVerifierOnChain alongside DAVerifierECDSA with one-sentence mechanism description"
    - "README deployment sections show both DA verifiers deployed in correct order"
    - "README environment variables include staging-specific env vars"
  artifacts:
    - path: "artifacts/StateOracle.json"
      provides: "Updated StateOracle ABI with DA verifier registry functions"
      contains: "addDAVerifier"
    - path: "README.md"
      provides: "Updated documentation covering DA verifier registry"
      contains: "DAVerifierOnChain"
  key_links:
    - from: "README.md"
      to: "script/DeployCore.s.sol"
      via: "deployment steps and console output match actual script"
      pattern: "DA Verifier.*OnChain.*deployed at"
    - from: "README.md"
      to: "src/StateOracle.sol"
      via: "documented governance functions match actual contract"
      pattern: "addDAVerifier|removeDAVerifier"
---

<objective>
Regenerate ABI artifacts and update README documentation to reflect the DA verifier registry, new governance functions, updated addAssertion signature, DAVerifierOnChain usage, and revised deployment flow.

Purpose: Complete the milestone by ensuring published artifacts and user-facing documentation accurately reflect all Phase 1-3 contract changes.
Output: Updated artifacts/ directory and README.md with comprehensive DA verifier registry documentation.
</objective>

<execution_context>
@/Users/fredo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fredo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation-abi-artifacts/04-CONTEXT.md
@.planning/phases/04-documentation-abi-artifacts/04-RESEARCH.md

@README.md
@shell/create_artifacts.sh
@script/DeployCore.s.sol
@script/DeployCoreWithStaging.s.sol
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate ABI artifacts</name>
  <files>artifacts/StateOracle.json, artifacts/DAVerifierECDSA.json, artifacts/AdminVerifierOwner.json, artifacts/interfaces/IDAVerifier.json, artifacts/interfaces/IAdminVerifier.json, artifacts/interfaces/IBatch.json, artifacts/libraries/AdminVerifierRegistry.json</files>
  <action>
Run `npm run prepare` to regenerate all ABI artifacts from the current forge build output. This executes `shell/create_artifacts.sh` which extracts ABIs for all published contracts.

Do NOT modify `shell/create_artifacts.sh` -- per user decision, the script already covers all contracts that should be published. DAVerifierOnChain, DAVerifierRegistry library, and AdminVerifierWhitelist are intentionally excluded from artifacts (interfaces are sufficient).

After regeneration, verify the updated StateOracle ABI includes:
- `addDAVerifier(IDAVerifier)` function
- `removeDAVerifier(IDAVerifier)` function
- `isDAVerifierRegistered(IDAVerifier)` function
- Updated `addAssertion` with 5 parameters (address, IDAVerifier, bytes32, bytes, bytes)
- `DAVerifierAdded` event
- `DAVerifierRemoved` event
- `DAVerifierNotRegistered` error
- `InvalidDAProof` error

Run `forge test` to confirm no accidental contract changes were introduced.
  </action>
  <verify>
    <automated>cd /Users/fredo/code/phylax/credible-layer-contracts && npm run prepare && forge test</automated>
  </verify>
  <done>artifacts/ directory contains freshly generated ABI files. StateOracle.json includes all new DA verifier registry functions, events, and errors. forge test passes confirming no regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Update README with DA verifier registry documentation</name>
  <files>README.md</files>
  <action>
Update README.md by expanding existing sections (no new top-level sections per user decision). Match the existing mid-detail tone: 1-2 paragraphs plus bullet lists, no Solidity code blocks. Leave the ASCII architecture diagram as-is.

**Section 1: "State Oracle Behavior" (current lines 38-43)**
Add a bullet point about managers selecting a registered DA verifier when adding assertions. Previously there was a single immutable DA verifier; now it is per-assertion from a governance-managed registry.

**Section 2: "Data Availability Verification" (current lines 51-64)**
Expand to explain the DA verifier registry concept: multiple DA verifiers can be registered by governance. List the two available verifiers:
- `DAVerifierECDSA`: requires a signature over the assertion ID from a configured `DA_PROVER_ADDRESS` (existing text, keep the Assertion DA link)
- `DAVerifierOnChain`: validates that `keccak256(proof) == assertionId`, proving on-chain bytecode availability (one sentence, matching ECDSA description style)
Mention that managers select which registered DA verifier to use when adding each assertion.

**Section 3: "State Oracle Administration" (current lines 66-74)**
Add a fourth bullet: "Add or remove DA verifiers: Adjust which data availability verification modules are authorized for use when adding assertions." Match the style of the existing admin verifier bullet.

**Section 4: "Deployment" paragraph (current lines 76-78)**
Update to mention both DAVerifierECDSA and DAVerifierOnChain. Reference `DeployCoreWithStaging.s.sol` alongside the existing scripts. Note that both DA verifiers are deployed and registered in the DA verifier registry during initialization.

**Section 5: "Environment Variables" (current lines 80-91)**
Add two staging-specific environment variables with a note that they apply only to `DeployCoreWithStaging.s.sol`:
- `STAGING_STATE_ORACLE_MAX_ASSERTIONS_PER_AA`
- `STAGING_STATE_ORACLE_ASSERTION_TIMELOCK_BLOCKS`

**Section 6: "Deployment Overview" (current lines 92-110)**
Update the numbered steps to reflect the actual deployment order from `DeployCore.run()`:
1. Deploy the DA verifier (ECDSA) and log its address.
2. Deploy the DA verifier (OnChain) and log its address.
3. Deploy `AdminVerifierOwner` if enabled and log its address.
4. Deploy `AdminVerifierWhitelist` if enabled and log its address.
5. Deploy the `StateOracle` implementation and log its address.
6. Deploy the proxy, initialize it with the configured admin verifiers and DA verifiers, and log the proxy address.

Update the console output example to include the OnChain DA verifier line. Use these labels matching the actual `console2.log` strings in DeployCore.s.sol:
```
DA Verifier deployed at <address>
DA Verifier (OnChain) deployed at <address>
Admin Verifier (Owner) deployed at <address>
Admin Verifier (Whitelist) deployed at <address>
State Oracle Implementation deployed at <address>
State Oracle Proxy deployed at <address>
```

**Section 7: "Testing with Anvil" console output (current lines 170-179)**
Add the `DA Verifier (OnChain) deployed at <address>` line after the ECDSA verifier line. Use a placeholder address for the new entry. Keep existing CreateX-derived addresses unchanged for the other contracts.
  </action>
  <verify>
    <automated>cd /Users/fredo/code/phylax/credible-layer-contracts && grep -q "DAVerifierOnChain" README.md && grep -q "addDAVerifier" README.md && grep -q "DA Verifier (OnChain)" README.md && grep -q "STAGING_STATE_ORACLE" README.md && echo "README verification passed"</automated>
  </verify>
  <done>README.md contains: (1) DA verifier registry concept in Data Availability section, (2) DAVerifierOnChain description alongside ECDSA, (3) per-assertion verifier selection in State Oracle Behavior, (4) DA verifier governance bullet in Administration, (5) updated deployment paragraph mentioning both verifiers and staging script, (6) staging env vars in Environment Variables, (7) updated deployment steps with OnChain verifier, (8) updated console output examples in both Deployment Overview and Testing with Anvil sections.</done>
</task>

</tasks>

<verification>
- `npm run prepare` succeeds without errors
- `forge test` passes (no regressions)
- `grep -c "DAVerifierOnChain" README.md` returns at least 2 (Data Availability section + deployment section)
- `grep -c "addDAVerifier" README.md` returns at least 1 (Administration section)
- `grep "DA Verifier (OnChain)" README.md` matches in deployment overview and testing sections
- `grep "STAGING_STATE_ORACLE" README.md` matches both staging env vars
- No new top-level sections added to README (section count unchanged)
</verification>

<success_criteria>
1. artifacts/ directory is freshly generated with `npm run prepare` and contains StateOracle ABI with all Phase 1-3 changes (new functions, events, errors, updated addAssertion signature)
2. README.md accurately documents the DA verifier registry, both DA verifiers, per-assertion selection, governance functions, updated deployment flow, staging env vars, and updated console output
3. forge test passes confirming no contract regressions
4. Documentation tone matches existing README: mid-detail, paragraph + bullet list format, no Solidity code snippets
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-abi-artifacts/04-01-SUMMARY.md`
</output>
